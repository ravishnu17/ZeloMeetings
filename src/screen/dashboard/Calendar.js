import React from 'react'
import { ScrollView, Text } from 'react-native'

const Calendar = () => {
  return (
    <ScrollView>
        <Text>Calendar</Text>
    </ScrollView>
  )
}

export default Calendar