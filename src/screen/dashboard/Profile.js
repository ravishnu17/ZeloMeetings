import React from 'react'
import { ScrollView, Text } from 'react-native'

const Profile = () => {
  return (
    <ScrollView>
        <Text>Profile</Text>
    </ScrollView>
  )
}

export default Profile
